// Scroll smoothly to a section when a button is clicked
function scrollToSection(sectionId) {
    document.querySelector(sectionId).scrollIntoView({
      behavior: 'smooth'
    });
  }
  
  // Optional: Add form submission logic or animation effects if needed
  